{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxi dev --port 2000",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "lint": "eslint --ext .ts,.vue --fix && prettier --write ."
  },
  "devDependencies": {
    "eslint": "^8.26.0",
    "eslint-plugin-vue": "^9.6.0",
    "nuxt": "3.0.0-rc.12"
  }
}
